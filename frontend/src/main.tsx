import React from 'react'
import ReactDOM from 'react-dom/client'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1>Olá, Campeonato de Trading!</h1>
    <p>O frontend React está funcionando.</p>
  </React.StrictMode>,
)
